import { css } from "lit";

export default css`
.dx-svgicon {
    width: var(--dx-icon-size-md);
    height: var(--dx-icon-size-md);
    color: var(--dx-icon-color);
    fill: currentColor;
}

.dx-svgicon:hover {
    color: var(--dx-icon-hover-color);
}
`